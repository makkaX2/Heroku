[phases.setup]
nixPkgs = ["...", "python311Full", "git", "ffmpeg"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "python -m heroku"
